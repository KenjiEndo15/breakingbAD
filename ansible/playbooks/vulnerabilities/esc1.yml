- name: ESC1
  hosts: dc01
  vars:
    action: "{{ action }}" # extra-vars
  vars_files:
    - ../../vars/breakingbad.json

  roles:
    - role: ../../roles/vulnerabilities/esc1/enable
      when: action == 'enable'

    - role: ../../roles/vulnerabilities/esc1/disable
      when: action == 'disable'
  gather_facts: false